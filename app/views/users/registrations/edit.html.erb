<div class="container">
  <div class="sub-container shadow bg-white rounded">
    
    <%= form_with model: @user, url: user_registration_path, local: true do |f| %>
    <%= f.hidden_field :family_id, value: current_user.family_id %>
    <%= f.hidden_field :host_user, value: current_user.host_user %>
    
    <!--<div class="text-center pt-4 mb-0">-->
      <!--<h6>編集ページ</h6>-->
    <!--</div>-->
    <div class="form p-5">
        <div class="form-group row">
            <label for="text" class="col-sm-4 col-form-label">ユーザーネーム</label>
            <%= f.text_field :nickname, class: "col-sm-8 form-control #{"is-invalid" if f.object.errors.messages[:nickname].present? }" %>
            <div class="invalid-feedback">名前を入力してください。</div>
        </div>
        <div class="form-group row mt-2">
          <label for="text" class="col-sm-4 col-form-label">メールアドレス</label>
          <%= f.text_field :email, autocomplete: "email", class: "col-sm-8 form-control #{"is-invalid" if f.object.errors.messages[:email].present? }" %>
          <div class="invalid-feedback">emailを入力してください。</div>
        </div>
        <!--<div class="form-group row mt-2">-->
        <!--  <label for="current_password" class="col-sm-4 col-form-label">パスワード(6桁)</label>-->
        <!--  <= f.password_field :current_password, value: "#{current_user.password}", autocomplete: "current-password", class: "col-sm-8 form-control #{"is-invalid" if f.object.errors.messages[:password].present? }" %>-->
        <!--  <div class="invalid-feedback">6桁の半角英数字で入力してください。</div>-->
        <!--</div>-->
        
        
  <!--<div class="field">-->
  <!--  <= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />-->
  <!--  <= f.password_field :current_password, autocomplete: "current-password", :class => "form-control" %>-->
  <!--</div>-->
        
        <!--<div class="form-group row mt-2">-->
        <!--  <p>ユーザーアイコンに指定するものを１つ選択してください。</p>-->
        <!--  <div class="user-icon-area">-->
        <!--    <= f.collection_radio_buttons :user_icon_id, UserIcon.all, :id, :images do |t| %>-->
        <!--      <div class="text-center">-->
        <!--        <= t.label { t.radio_button } %>-->
        <!--        <img src="/user_images/<= t.text %>", class="user-icon-image" >-->
        <!--      </div>-->
        <!--    < end %>-->
        <!--  </div>-->
        <!--</div>-->
      </div>
        <div class="form-group">
          <%= f.submit "更新する", class: "btn new-submit-button p-2 w-100" %>
        </div>
      <% end %>
  
  </div>
</div>